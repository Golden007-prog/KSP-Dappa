# Phase 8 — data ingest (CSV → official ER schema) — feature lines

Backlog rows shipped: 209–232 (§7 of `docs/ROUND2_FEATURE_BACKLOG.md`). Row 233 (Stratus-drop ingest via an object-created Signal) was not built. Row 228 is partial (see the last infra line). Counting rule: Round-2 Appendix A — one scored line (row 214), everything else `[infra]`.

## Scored

1. "What changed" after a load — KPIs before/after the batch from the same AggMonthly aggregates `/summary/kpis` reads (FIRs this month, heinous cases, month-on-month, open alerts), the batch's rows by month, and every district × crime-head month the batch touches with its robust (MAD) z before and after and a "would raise an alert" status at z ≥ 2 — projected at validation, recomputed after the load; plain sentence first (`ingestDelta` glossary term) with the statistic behind the (i) — `client/src/routes/ingest/WhatChangedCard.jsx`, `functions/dappa_api/lib/ingest.js` (whatChanged), `client/src/lib/plainlanguage.js` [C3] (row 214)

## Infrastructure

2. `/ingest` route with a five-step flow (table → file → map → check → load) as scrollable tabs, mobile-first at 360×640, 44 px targets, nav item under Records — `client/src/routes/Ingest.jsx`, `client/src/App.jsx`, `client/src/components/Layout.jsx` [infra] (row 223)
3. ER table picker over the 27 schema tables in console-import order (19 reference masters, 7 case tables, Appendix C), each card naming the tables it needs first and its PII / never-used column counts — `client/src/routes/ingest/TablePicker.jsx`, `GET /ingest/tables` [infra] (row 223)
4. Official column-template CSV per table (`GET /ingest/template/:table.csv`, `?example=1` adds a sample row); built client-side in the static demo — `functions/dappa_api/lib/routes/ingest.js`, `client/src/routes/ingest/TablePicker.jsx` [infra] (row 217)
5. File drop / picker with a quote-aware RFC-4180 parser, delimiter sniffing (comma / semicolon / tab / pipe), UTF-8 BOM and UTF-16 BOM handling, Windows-1252 fallback reported as such, ragged / short row counts and Kannada-cell count — `client/src/lib/csv.js`, `client/src/routes/ingest/FileStep.jsx` [infra] (row 221)
6. "Use the bundled sample" button that loads the 200-row demo file from `client/public/ingest_demo/` — `client/src/routes/ingest/FileStep.jsx`, `scripts/make_ingest_demo.mjs` [infra] (row 222)
7. Column auto-map by exact header, CCTNS-style preset, known aliases, then normalised Levenshtein ≥ 0.72, each match labelled with how it was made and its score — `client/src/routes/ingest/automap.js`, `client/src/routes/ingest/MappingStep.jsx` [infra] (row 225)
8. Manual per-column override and saved mapping templates in localStorage (save / apply / delete per table) — `client/src/routes/ingest/MappingStep.jsx`, `client/src/routes/ingest/mappingStore.js` [infra] (row 225)
9. CCTNS IIF-1 → CaseMaster mapping preset, flagged unverified in the registry (`verified:false`) and on screen because only the form list is verified — `functions/dappa_api/lib/ingest.js` (PRESETS) [infra] (row 232)
10. Privacy guard — caste / religion columns accepted for schema fidelity, blanked before load by default (untick to keep; still never used in analytics), name / DOB / KGID columns marked PII and never exported, extra headers classified (never-used / PII / not in schema) and dropped, shown before validation and again as "what the guard did" — `functions/dappa_api/lib/ingest.js`, `client/src/routes/ingest/MappingStep.jsx`, `client/src/routes/ingest/ResultPanel.jsx` [infra] (row 213)
11. Schema validation per row: required columns, Int / Double (sent as strings) / Boolean (1/0, yes/no) / Date / DateTime / Var Char (per-column limit, 255 cap) / Text (10,000) with reason codes — `functions/dappa_api/lib/ingest.js` (parseValue) [infra] (row 224)
12. Foreign-key existence against the live lookups unioned with a bundled reference copy of the masters (`assets/ingest_reference.json`: 38 districts, 359 units, 1,280 employees, 76 courts …) plus a page read for values neither knows; Employee / Accused references warn, the rest reject — `functions/dappa_api/lib/ingest.js` (buildReference, storeKeys) [infra] (row 224)
13. CrimeNo structure validator — 18 digits, 1 category · 4 district · 4 station · 4 year · 5 serial, checked against CaseCategoryID, the station's district, PoliceStationID and the registration year; CaseNo drift is a warning — `functions/dappa_api/lib/ingest.js` (parseCrimeNo, crimeNoIssues) [infra] (row 210)
14. Sub-head ↔ head consistency (CrimeMinorHeadID must belong to CrimeMajorHeadID) — `functions/dappa_api/lib/ingest.js` [infra] (row 224)
15. dd-mm-yyyy / dd/mm/yyyy / dd.mm.yyyy dates (with optional HH:MM[:SS]) normalised to ISO with a note; Indian lakh/crore digit grouping stripped from Int / Double with a note; U+FFFD replacement characters flagged — `functions/dappa_api/lib/ingest.js`, `client/src/lib/csv.js` [infra] (row 221)
16. Duplicate detection inside the batch (PK, CrimeNo, composite keys) and against the Data Store by IN-chunked page reads on CrimeNo and CaseMasterID (or the table's key), district / unit ids asked in both padded and bare forms — `functions/dappa_api/lib/ingest.js` [infra] (row 226)
17. Point-in-polygon check of latitude / longitude against the station's district polygon (ray cast over `assets/karnataka_districts.geojson`); invalid and out-of-Karnataka coordinates reject, out-of-district warns unless the strict option is on — `functions/dappa_api/lib/ingest.js` (pointInDistrict) [infra] (row 220)
18. Reference-data ordering enforced — a fact table whose masters are empty reports `prerequisites.missing` at validation and `409 ORDER_REFERENCE_FIRST` at load, naming the tables to load first — `functions/dappa_api/lib/ingest.js` (checkPrerequisites) [infra] (row 230)
19. Per-row verdict with reason codes (30 codes, each translated en / kn), an issue summary with counts and samples, and counts of accepted / rejected / accepted-with-notes — `functions/dappa_api/lib/ingest.js`, `client/src/routes/ingest/codes.js`, `client/src/locales/{en,kn}/ingest.js` [infra] (row 209)
20. Mapped preview of the first 50 rows with a StatusPill verdict per row and a mark per cell the checker commented on (✕ reject · ● warning · ℹ note, tooltip with the reason) — `client/src/routes/ingest/PreviewTable.jsx` [infra] (row 211)
21. Dry run — validation never writes; `POST /ingest/validate` returns `dryRun:true`, and the load is a separate call — `functions/dappa_api/lib/routes/ingest.js` [infra] (row 212)
22. Data-quality profile — blank rate per column, date range, station coverage (known / distinct / districts), coordinate sanity (with point / inside / outside / out of state / invalid), encoding notes — `functions/dappa_api/lib/ingest.js`, `client/src/routes/ingest/ResultPanel.jsx` [infra] (row 229)
23. Data Store budget check — rows → insert calls of ≤ 200, the 5,000-inserts-a-month free tier, rows loaded this month by the container; advisory because the Basic plan is active — `functions/dappa_api/lib/ingest.js` (budgetFor) [infra] (row 218)
24. Row-level rejection report as CSV (`GET /ingest/batches/:id/rejections.csv`): row number, key columns, verdict, codes, columns, details — never a PII or narrative column; built client-side in the static demo — `functions/dappa_api/lib/ingest.js` (rejectionCsv), `client/src/routes/ingest/ResultPanel.jsx` [infra] (row 209)
25. Multi-part validation upload (≤ ~700 KB JSON per part, `part:{batchId,index,final}`) so a 5,000-row file fits the function body limit and shows progress — `client/src/routes/ingest/ingestApi.js`, `functions/dappa_api/lib/ingest.js` (receive) [infra] (row 223)
26. Chunked load of accepted rows only (≤ 200 per `insertRows`, `chunkLimit` chunks per call) with a resume token (`<batchId>:<cursor>`, 409 on a stale cursor) and a progress bar that follows the token to completion — `functions/dappa_api/lib/ingest.js` (load), `client/src/routes/ingest/LoadStep.jsx` [infra] (row 227)
27. Load gate — District or State tier (`X-Dappa-Tier`) or the admin token; Data Store writes only with the admin token on the deployed API, otherwise memory storage, and `meta.storage` says which — `functions/dappa_api/lib/ingest.js` (loadAuthorization) [infra] (row 227)
28. Ingest audit row through phase 7's `recordAction` (subject `ingest:<batchId>`, who / table / accepted / rejected / storage), falling back to a direct `ActionLog` insert and then memory — `functions/dappa_api/lib/ingest.js` (auditRow) [infra] (row 215)
29. Batch history (`GET /ingest/batches`, `GET /ingest/batches/:id`) — status pill, counts, storage, resume token, rejection link; the two-hour container TTL is stated on screen — `client/src/routes/ingest/BatchHistory.jsx` [infra] (row 219)
30. Batch rollback by id (`POST /ingest/batches/:id/rollback`) — memory batches always; Data Store batches only by the ROWIDs this container recorded, with the admin token; anything else is a 409 that says why — `functions/dappa_api/lib/ingest.js` (rollback) [infra] (row 231)
31. Static-demo degrade — GET registry from the snapshot, validation in the browser (a labelled subset: no FK, store-duplicate, polygon or KPI checks), loads refused with a visible note — `client/src/routes/ingest/localValidate.js`, `client/src/routes/ingest/ingestApi.js`, `scripts/demo_snapshot.mjs` [infra] (row 223)
32. Demo dataset — `data/ingest_demo/CaseMaster_sample.csv` (200 rows: 183 clean July-2026 rows with new CaseMasterIDs 90001+ and CrimeNo serials 90001+, 12 rejected and 5 accepted-with-a-note rows, BOM + CRLF) with `README.md` listing every special row, regenerated by `scripts/make_ingest_demo.mjs` — `data/ingest_demo/`, `scripts/make_ingest_demo.mjs` [infra] (row 222)
33. Contract tests — 122 checks covering the registry, the CrimeNo validator, date / int / boolean / length parsing, polygons, the privacy guard, validation 400s, a mixed batch with one of every defect, the rejection CSV, the tier gate, a chunked memory load with resume tokens, rollback, reference ordering, child-table parent checks and the demo dataset itself — `functions/dappa_api/test/round2/phase8-ingest.test.mjs` [infra] (row 216)
34. Post-load recompute, partial — the z-check the batch would trip is computed for every touched district × crime-head month (top 12 by row count); no batch AggMonthly upsert path was added: on the deployed store the CaseMaster insert Signal upserts per row and the nightly job reconciles, in memory storage nothing to upsert — `functions/dappa_api/lib/ingest.js` (whatChanged) [infra] (row 228, partial)
