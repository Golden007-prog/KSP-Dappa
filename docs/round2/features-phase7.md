# Phase 7 — action loop and audit trail (FEATURES.md candidates)

One line per feature in FEATURES.md style; `[infra]` lines are excluded from every capability count (Round-2 Appendix A). Backlog rows are from `docs/ROUND2_FEATURE_BACKLOG.md` §3. Scored lines: 8. Infra lines: 17.

## Scored

1. Alert precision from officer feedback — true_positive ÷ labelled outcomes with a 95 % Wilson score interval, overall and per severity band and per crime head, rendered as the plain sentence "of the N alerts an officer labelled, k turned out real — about p of every 100 … could be anywhere from lo to hi" with the formula behind the (i) (backlog row 106; `docs/COMPETITIVE_SCAN.md` §6 Geolitica) — `functions/dappa_api/lib/actionlog.js` (`computeOutcomes`, `wilson`), `client/src/routes/alerts/OutcomePanel.jsx`, `client/src/lib/plainlanguage.js` (`precision`) [C6 C3]
2. Server-side time-to-acknowledge board from the ActionLog — hours from `AnomalyAlert.CreatedAt` to the first acknowledge action, six-bucket distribution (under 1 h … over 3 days), median overall and per severity / crime head, plain sentence "half of the acknowledged alerts were looked at within h hours" (backlog row 98 — replaces the browser-timed SLA badges as the printed truth) — `functions/dappa_api/lib/actionlog.js`, `client/src/routes/alerts/OutcomePanel.jsx`, `client/src/lib/plainlanguage.js` (`timeToAck`) [C3]
3. "What happened to past alerts" panel on /alerts — per window (all / 30 / 90 / 365 days) and per severity and crime head: alerts, acknowledged, assigned, escalated, dismissed, nobody-touched, open-past-window, with the outcome mix (real rise · false alarm · already known · acted on · no action needed) (backlog row 105) — `client/src/routes/alerts/OutcomePanel.jsx`, `GET /alerts/outcomes` in `functions/dappa_api/lib/routes/actionlog.js` [C3]
4. Staleness: open alerts nobody has touched, each with age against its severity's triage window (critical 4 h · high 12 h · medium 24 h · low 48 h) and a past-window flag, counted per severity and listed oldest-within-severity first (backlog row 113) — `functions/dappa_api/lib/actionlog.js` (`SLA_HOURS`, `untouched`), `client/src/routes/alerts/OutcomePanel.jsx` [C3]
5. Dismissal-reason taxonomy (duplicate · known cause · data error · seasonal · other-with-note) and its distribution — reason is mandatory on every dismiss, so the chart is the real reason mix (backlog row 94) — `functions/dappa_api/lib/actionlog.js` (`DISMISS_REASONS`), `client/src/routes/alerts/ActionControls.jsx`, `client/src/routes/alerts/OutcomePanel.jsx` [C3]
6. Labels-for-learning block — how many outcome labels exist, per class, the shortfall against the stated minimum (30 per class, 100 in all) and the sentence that a retrain is `pipeline/analytics.py` re-run by a person with these labels as the test set; `onlineLearning:false` is in the payload (backlog row 92) — `functions/dappa_api/lib/actionlog.js` (`labels`), `client/src/routes/alerts/OutcomePanel.jsx` [C6]
7. Decision record on every alert — the timeline of acknowledge / assign / escalate / dismiss / note / outcome rows on the card and the detail sheet, each with actor, role, time, the engine holding it (Data Store · demo fixture · this session) and a demo-seed marker, summarised as status pills (acknowledged · assigned: unit · escalated · dismissed · outcome) (backlog rows 89, 91, 99) — `client/src/routes/alerts/ActionTimeline.jsx`, `client/src/routes/alerts/ActionControls.jsx`, `GET /alerts/:alertKey/actions` [C3]
8. Action-loop digest per unit or district at /alerts/digest — open alerts, decisions recorded in the window by type, precision and median time-to-acknowledge, the untouched list and the highest-risk stations, from the same JSON Catalyst Mail sends; printable, read-aloud, provenance-stamped (backlog row 116) — `client/src/routes/AlertsDigest.jsx`, `functions/dappa_api/lib/actiondigest.js`, `GET /alerts/digest` [C3]

## Infra

9. `ActionLog` write path: `POST /alerts/:alertKey/actions` with the action-type enum, dismiss-requires-reason, outcome-requires-label, escalate-to-next-tier default, assign-requires-assignee, note length limit, ISO clientTs validation and an idempotency key over (subject, action, actor, clientTs) so a retried POST cannot double-count (backlog rows 88, 104) — `functions/dappa_api/lib/actionlog.js`, `functions/dappa_api/lib/routes/actionlog.js` [infra]
10. Storage honesty: SDK `datastore().table('ActionLog').insertRow` when the function runs on Catalyst, a per-container memory store otherwise; reads merge ZCQL rows (or the PUBLIC_DEMO fixture) with memory rows and `meta.storage` / `meta.source` name the engine (`datastore` · `fixture` · `local`) — `functions/dappa_api/lib/actionlog.js` (`listActions`, `sourceOf`) [infra]
11. Actor resolution: Catalyst Authentication identity when a session exists (`lib/auth.js whoami`), the demo admin token, or the tier switcher's role plus a client-supplied display name, recorded in `Payload.actorSource`; `AnomalyAlert.Status` flips on acknowledge/dismiss only for an authenticated or admin-token caller and the response says so (`statusUpdated`, `statusReason`) — `functions/dappa_api/lib/routes/actionlog.js` (`resolveIdentity`) [infra]
12. `GET /alerts/:alertKey/actions` timeline + summary and `GET /actions/recent?unit=&days=&subjectType=&limit=` newest-first feed over every subject type (alert · case · alias · face · ingest) — `functions/dappa_api/lib/routes/actionlog.js` [infra]
13. `GET /alerts/outcomes?window=&unit=` cached 60 s and busted by every write through an epoch key — `functions/dappa_api/lib/routes/actionlog.js` [infra]
14. `GET /alerts/digest?unit=&days=` JSON and `POST /alerts/digest/send` through `lib/mail.js sendDigest` behind FEATURE_MAIL, returning `mode` (disabled · not-configured · sent · error-fallback), the preview and a `fallback.page` pointer when nothing was sent — `functions/dappa_api/lib/actiondigest.js`, `functions/dappa_api/lib/routes/actionlog.js` [infra]
15. Escalate / assign push through `lib/push.js sendPush` behind FEATURE_PUSH, with the push mode reported in the action response (backlog row 96) — `functions/dappa_api/lib/routes/actionlog.js` [infra]
16. Action controls on every alert card and on the detail sheet — six actions with 44-px targets on the Beat / Station tiers, the framing sentence, the actor-name field, form validation mirroring the server, toasts that say which engine kept the record — `client/src/routes/alerts/ActionControls.jsx`, mounted by one line each in `client/src/routes/alerts/AlertCard.jsx` and `client/src/routes/alerts/AlertDetailSheet.jsx` [infra]
17. In-app notification centre — topbar bell and More-sheet row with the unread count as text + glyph, a sheet listing new alerts, escalations (flagged "for your tier"), assignments, outcomes and dismissals, polled every minute from `/alerts?status=OPEN` and `/actions/recent`, per-browser read state — `client/src/components/NotificationBell.jsx`, `client/src/lib/notifications.js`, mounted by one line each in `client/src/components/Layout.jsx` [infra]
18. Web-push registration behind FEATURE_PUSH — loads the project's own Catalyst web SDK (`/__catalyst/sdk/init.js`), calls `catalyst.notification.enableNotification()` and `POST /notify/register` for the signed-in email; off the flag or without a session the sheet says so and the in-app list carries the same events — `client/src/components/NotificationBell.jsx` [infra]
19. Deterministic demo seed — 40 fixture-marked decisions on the oldest alerts (eight decision templates cycled, first-action delays 0.5–50 h), shared by the contract fixture and the CSV generator, written to `pipeline/out/ActionLog_seed.csv` from the live alert list — `functions/dappa_api/lib/actionlog.js` (`seedActions`), `functions/dappa_api/lib/fixture.js`, `scripts/seed_actionlog.mjs` [infra]
20. `recordAction` accepts the other phases' shapes — a raw ActionLog column row (OCR attach), `(ctx, req, input)` without an identity (ingest batches) and the four-argument form (face decisions), with a loose vocabulary for non-alert subjects and a `bySubject` block in the outcomes so those audit rows are counted apart, never in alert precision — `functions/dappa_api/lib/actionlog.js` [infra]
21. `actions` locale namespace (175 keys, en + kn) and the two new glossary terms (`precision`, `timeToAck`) in both languages — `client/src/locales/en/actions.js`, `client/src/locales/kn/actions.js`, `client/src/lib/plainlanguage.js` [infra]
22. Framing sentence on every action surface (controls, panel, notification centre, digest), en + kn: "DAPPA never tells an officer what to do; it shows what it found and how sure it is — the decision and its record are the officer's." — `client/src/locales/*/actions.js` (`framing`) [infra]
23. Route `/alerts/digest` registered lazily under `<Boundary>` with URL-driven `unit` / `days` — `client/src/App.jsx`, `client/src/routes/AlertsDigest.jsx` [infra]
24. Loading, empty and error states on the panel, timeline, notification sheet and digest page — `client/src/routes/alerts/OutcomePanel.jsx`, `client/src/routes/alerts/ActionTimeline.jsx`, `client/src/components/NotificationBell.jsx`, `client/src/routes/AlertsDigest.jsx` [infra]
25. Service map rows for Catalyst Mail and Push Notifications now name the action-loop call sites and endpoints (`/alerts/digest`, `/alerts/digest/send`, `/alerts/:alertKey/actions`, `/actions/recent`) — `functions/dappa_api/lib/servicemap.js` [infra]
